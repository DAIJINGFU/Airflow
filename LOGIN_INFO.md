# Airflow 3.x 登录信息

## ✅ 成功安装！

Airflow 3.1.3 已成功安装并运行在 **Standalone 模式**。

---

## 🔐 登录凭据（已验证 ✅）

**访问地址**: http://localhost:8080

**登录信息**:

- **用户名**: `admin`
- **密码**: `SNZ5mDTmNdBDT2bS`
- **API 状态**: ✅ 认证测试通过

> ⚠️ **重要**: 密码在容器完全删除重建时会改变（`restart` 不会改变密码）

---

## 🌐 浏览器登录注意事项

如果无法在浏览器中登录，请按以下步骤操作：

### ✅ 推荐方法（最快）

**使用隐私/无痕模式打开浏览器**:

- Chrome: `Ctrl + Shift + N`
- Edge: `Ctrl + Shift + P`
- Firefox: `Ctrl + Shift + P`
- 访问 http://localhost:8080
- 使用上述密码登录

### 备选方法

1. **清除浏览器缓存**: F12 → Application → Clear Storage
2. **硬刷新页面**: `Ctrl + Shift + R` 或 `Ctrl + F5`
3. **尝试其他浏览器**: Edge、Firefox 等

> 💡 **原因**: 浏览器可能缓存了旧的认证信息，导致与新的 secret_key 冲突

---

## 📝 重要说明

### 使用 Standalone 模式的原因

Airflow 3.x 改变了认证系统，标准的 Docker Compose 配置需要额外的用户创建步骤。为了快速启动和测试，我们使用了 **Standalone 模式**，它会：

1. ✅ 自动创建管理员用户
2. ✅ 在单个容器中运行所有组件
3. ✅ 适合开发和测试环境
4. ✅ 自动生成安全的随机密码

### 当前运行的服务

```bash
docker ps | grep airflow
```

你应该能看到:

- `airflow_new-airflow-standalone-1` - 运行中并且健康

### 如何查看密码

如果忘记密码，可以从日志中找到：

```bash
docker logs airflow_new-airflow-standalone-1 2>&1 | grep "Password for user"
```

---

## 🚀 快速操作

### 启动服务

```bash
docker-compose -f docker-compose-standalone.yml up -d
```

### 停止服务

```bash
docker-compose -f docker-compose-standalone.yml down
```

### 查看日志

```bash
docker logs -f airflow_new-airflow-standalone-1
```

### 重启服务

```bash
docker-compose -f docker-compose-standalone.yml restart
```

---

## 📚 文件说明

- `docker-compose-standalone.yml` - Standalone 模式配置（当前使用）
- `docker-compose.yml` - 完整的生产环境配置（多容器）
- `dags/` - DAG 文件目录
- `logs/` - 日志目录
- `plugins/` - 插件目录

---

## ⚠️ 版本提醒

**此环境使用 Airflow 3.1.3**  
请勿降级到 2.x 版本！

Airflow 3.x 主要变更：

- `webserver` 命令改为 `api-server`
- 新的认证系统 (SimpleAuthManager / FAB AuthManager)
- 改进的 API 和性能

---

**安装时间**: 2025-11-24  
**状态**: ✅ 运行正常
