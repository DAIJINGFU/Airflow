# Airflow 环境配置文件
# 重要：此配置用于 Airflow 3.x 版本

# Airflow UID (Windows/WSL 用户可以使用 50000)
AIRFLOW_UID=50000

# Airflow Web UI 用户凭证
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=airflow

# 额外的 Python 包 (如需要，可在此添加)
_PIP_ADDITIONAL_REQUIREMENTS=

# 项目目录 (默认为当前目录)
AIRFLOW_PROJ_DIR=.

# QLib ???? (???????????)
QLIB_DATA_HOST_DIR=../airflow/stockdata/qlib_data
